#include <stdio.h>
#include "Q4.h"

int Q4(int arr[])
{
	if (leftEtoRight(arr, 10))
		printf("The array answers the requirement");
	else
		printf("The array dose not answers the requirement");
			
	return 0;
}
